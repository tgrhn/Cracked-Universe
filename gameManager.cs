using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameManager : MonoBehaviour
{
   public float playerSpeed;
    public bool atlaButton;
    public bool atlamaButton;
    void Start()
    {
        atlaButton = false;
        atlamaButton = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
